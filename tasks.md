# مهام تكامل البكسل والتسويق الرقمي والسيو المتقدم (Phase 46 & 47)

- [x] **تكامل بكسل التواصل الاجتماعي (Phase 46)**:
  - إتاحة حقول لتخزين (Facebook Pixel ID, TikTok Pixel ID, Google Analytics ID) في قاعدة بيانات إعدادات التاجر (Store Settings).
  - تعديل متحكم الإعدادات لوحة التحكم `app/Http/Controllers/Merchant/SettingController.php` و `app/Http/Controllers/SettingController.php` لتخزين ومعالجة الحقل الجديد `google_analytics_id`.
  - تحديث واجهة React/Inertia في لوحة التحكم للتاجر `resources/js/Pages/Merchant/Settings/Index.jsx` لإضافة حقل إدخال Google Analytics ID وحفظه بنجاح.
  - نقل صفحات الـ HTML الخاصة بالـ Storefront من المجلد الاستاتيكي `public/shop` إلى مجرد قوالب في `resources/views/shop` لخدمتها ومعالجتها برمجياً وديناميكياً من خلال Laravel.
  - إنشاء متحكم `StorefrontController` لمعالجة طلبات صفحات المتجر وحقن أكواد تتبع بكسل Meta (فيسبوك)، وتيك توك، وجوجل أناليتكس ديناميكياً داخل الـ Head والـ Body.

- [x] **إعداد SEO المتقدم (Phase 47)**:
  - توليد ملف `sitemap.xml` ديناميكي لكل متجر (tenant) يحتوي على روابط المنتجات والأقسام والصفحات الأساسية.
  - إعداد ملف `robots.txt` ديناميكي يوجه محركات البحث إلى خريطة الموقع الخاصة بالمتجر ويمنع أرشفة لوحة التحكم والمناطق الحساسة مثل الدفع.
  - حقن بيانات منظمة Structured Data بصيغة JSON-LD الخاصة بالمنتج في صفحة تفاصيل المنتج `product.html` ديناميكياً لتحسين ظهور المنتج في محركات البحث.
  - تمكين أرشفة المتجر عبر تغيير وسم `robots` إلى `index, follow` بدلاً من `noindex` في الصفحات العامة.

- [x] **تأكيد سلامة البناء وتطهير الكاش**:
  - تشغيل `php artisan config:clear` لتنظيف الكاش.
  - تشغيل `npm run build` لبناء وتأكيد سلامة الفرونت إند وملفات React/Inertia.

- [x] **إعداد نظام Localization متكامل ودعم RTL/LTR ديناميكي (Phase 48)**:
  - إنشاء وسيط `SetLocale` لتحديد لغة التطبيق وتخزينها بالجلسة (Session) أو عبر Query parameter.
  - إضافة مسار تحويل اللغة `/lang/{locale}` ومتحكم `LanguageController` لسهولة تبديل اللغات.
  - إدراج محول اللغة (Language Switcher) في لوحة تحكم التاجر (Sidebar والمينو المتجاوب) وتعديل الهيكل ليدعم RTL/LTR ومرونة العرض تلقائياً.
  - ترجمة وتدويل المتجر العام (Storefront) ديناميكياً عبر قاموس ترجمة متكامل في `public/shop/shared.js` وتبديل الاتجاه `dir` تلقائياً.
  - تفعيل تحديث اللغة وتزامنها مع جلسة السيرفر عند تغيير لغة المتجر لضمان تناسق البيانات.

- [x] **تطوير قائمة المنتجات والفلترة المتقدمة والفرز (Phase 53)**:
  - إضافة خيارات الفلترة المتقدمة: الفرز حسب القسم (Category Chips)، شريط فلترة الأسعار التفاعلي (Price Range Input)، وخيارات الفرز المتعددة (الأحدث، الأرخص، الأغلى، الأكثر خصماً).
  - إعداد نظام التحميل التلقائي/اللانهائي (Infinite Scroll) مع زر "تحميل المزيد" لتسهيل تصفح المنتجات.
  - تفعيل وتأكيد عمل زر الإضافة المباشرة إلى السلة من بطاقة المنتج.
  - تحديث ملفات الترجمة لتدويل الفلاتر والفرز ودعم اللغتين العربية والإنجليزية ديناميكياً.

- [x] **بناء وتنسيق أقسام الصفحة الرئيسية ديناميكياً (Phase 52)**:
  - تعديل الكنترولر `StorefrontController.php` لجلب ترتيب ومحتويات أقسام الصفحة الرئيسية (Hero Slider, Featured Categories, Best Offers, Latest Products) المحددة من إعدادات المتجر الحالي.
  - رندرة الأقسام ديناميكياً وترتيبها في صفحة `index.html` بناءً على الإعدادات.
  - التأكد من ربط وتحديث صور الـ Slider والبانرات الإعلانية لكل متجر بشكل ديناميكي كامل.
  - تحديث ملف الجافا سكربت `shared.js` لإضافة وظيفة رندرة الأقسام وترتيبها.
  - تشغيل `php artisan config:clear` و `npm run build` والتأكد من عدم وجود أي تعارض.

- [x] **تطوير صفحة تفاصيل المنتج والخيارات والمتغيرات (Phase 54)**:
  - تعديل قاعدة البيانات عبر إضافة هجرة (Migration) جديدة لإنشاء أعمدة المقاسات `sizes` والألوان `colors` في جدول المنتجات.
  - بناء معرض صور تفاعلي متميز يدعم تأثير التكبير عند التمرير (Hover Zoom) مع حركات تمرير اللمس على الهواتف الذكية (Mobile Swiping Transitions).
  - تفعيل واجهة اختيار المتغيرات (Sizes & Colors Selector) بأزرار تفاعلية أنيقة وتحديث حالة المخزون المعروض فورياً بناءً على الخيار المحدد (متوفر / قارب على النفاد / نفد).
  - تحديث السعر المعروض فورياً بناءً على المتغير المختار في حال وجود سعر خاص بالمتغير.
  - جلب وعرض المنتجات المشابهة (Related Products) ديناميكياً أسفل الصفحة بناءً على تصنيف المنتج الحالي.
  - تشغيل `npm run build` و `php artisan config:clear` للتأكد من سلامة البناء وتطهير الكاش.

- [x] **نظام السلة المحسّن (Phase 55)**:
  - حفظ السلة في قاعدة البيانات للمستخدمين المسجلين وفي Session/localStorage للزوار مع دمج السلتين تلقائياً عند تسجيل الدخول في StorefrontCartController.
  - تطوير واجهة وديناميكية صفحة cart.html بدعم تعديل الكميات، حذف المنتجات، إضافة وإزالة كوبون الخصم، عرض ملخص الطلب، وميزة حفظ السلة للاحقاً ونقلها مجدداً للسلة.

- [x] **صفحة الدفع المحسّنة (Phase 56)**:
  - دعم كامل للتحقق من الكوبونات وتطبيق الخصم في CheckoutController.php مع فحص المخزون (Stock Validation) قبل تأكيد الطلب.
  - دعم الطلب السريع كزائر (Guest Checkout) وخيار حفظ عنوان وبيانات الشحن في localStorage.
  - دعم طرق الدفع COD والتحويل الإلكتروني والتحقق الإلزامي من مربع الشروط والأحكام.

- [x] **نظام قائمة الأمنيات - Wishlist (Phase 57)**:
  - إنجاز النظام بالكامل وتفعيله في واجهة المتجر.

- [x] **نظام البحث المتقدم - Search (Phase 58)**:
  - إنجاز النظام بالكامل وتفعيله مع الاقتراحات والتاريخ وسجل البحث.

- [x] **نظام المراجعات المحسّن (Phase 59)**:
  - دعم الفرز والترتيب والفلترة حسب النجوم ورفع الصور مع المراجعة في ReviewController.php.
  - إضافة شارة المشتري المعتمد ودعم رد المتجر الرسمي وأزرار الإعجاب التفاعلية في واجهة المراجعات في product.html.

- [x] **صفحة حساب العميل (Phase 60)**:
  - بناء وتطوير AccountController.php لدعم إدارة الملف الشخصي، طلباتي مع الجدول الزمني لتتبع الحالة (Order Timeline)، عناوين الشحن، ربط المفضلة، تغيير كلمة المرور وحذف الحساب.
  - تصميم واجهة الحساب في account.blade.php بشكل ديناميكي ومتجاوب مع مودالات تفاعلية لعرض تفاصيل الطلبات وإدارة العناوين.

- [x] **تتبع الطلبات - Order Tracking (Phase 61)**:
  - بناء وتطوير OrderTrackingController لدعم تتبع الطلبات بدون تسجيل دخول عبر رقم الطلب والبريد/الهاتف، مع إرجاع الجدول الزمني (Timeline) لحالة الطلب.
  - تصميم واجهة التتبع في tracking.blade.php / tracking.html بشكل جذاب ومبهر يعرض حالة التوصيل وشريط التقدم وتفاصيل المنتجات.

- [x] **نظام صفحات الهبوط - Landing Pages (Phase 62)**:
  - إنشاء جدول landing_pages في قاعدة البيانات وموديل LandingPage بدعم متعدد المتاجر (Tenancy) وحساب معدل التحويل.
  - إنشاء LandingPageController و متحكم التاجر لإدارة ورندرة صفحات الهبوط بديناميكية تامة على الروابط المخصصة /lp/{slug}.
  - تصميم واجهة landing.blade.php بتصميم عصري وداعم للـ Dark Mode ومؤشرات التفاعل والعد التنازلي وشريط الشراء العائم.

- [x] **نظام العروض والتخفيضات - Promotions & Discounts (Phase 63)**:
  - إنشاء جدول promotions في قاعدة البيانات وموديل Promotion لدعم العروض المحدودة (Flash Sale)، الموسمية، التصفية وحزم المنتجات.
  - تطوير PromotionController ومتحكم التاجر لإدارة العروض وحساب السعر وتاريخ الانتهاء ديناميكياً.
  - تصميم واجهة promotions.blade.php بدعم مؤشر العد التنازلي وبادج الخصم على بطاقات المنتجات.

- [x] **نظام الإشعارات للعميل - Customer Notifications (Phase 64)**:
  - إنشاء خدمة CustomerNotificationService ومتحكم NotificationController لإدارة تفضيلات وإشعارات العملاء.
  - تجهيز كلاسات وقوالب البريد الإلكتروني (RTL) للحالات: تأكيد التسجيل، تأكيد الطلب، تحديث الحالة، العروض، توفر منتج بالمفضلة، وتذكير السلة المتروكة.

- [x] **تحسين الأداء والتجربة - Storefront Performance (Phase 65)**:
  - إنشاء Service Worker متكامل (service-worker.js) بدعم PWA وكاش الأصول الثابتة والعمل أوفلاين.
  - بناء مكتبة الأداء (performance.js) لتشغيل Lazy Loading، تحويل الصور لـ WebP، وتفعيل تأثير Shimmer Skeleton Loaders.
  - إنشاء ميدل وير OptimizeStorefrontResponse لكبس وضغط استجابة HTML وإضافة هيدرز الكاش والأداء.

- [x] **بنية نظام الثيمات - Theme Architecture (Phase 66)**:
  - تطوير خدمة ThemeService لتحميل الثيم النشط للمتجر وتوليد متغيرات CSS ديناميكياً مع Fallback للثيم الافتراضي.
  - إنشاء ThemeController لتدعيم التبديل اللحظي (Hot-swap) ووضع المعاينة الحية وتخصيص الألوان والخطوط.
  - إعداد ملف الهيكل الأساسي والمواصفات للثيمات theme-schema.json.

- [x] **الثيم الأول - Classic Store / Default (Phase 67)**:
  - بناء مجلد الثيم الافتراضي وتنسيقاته (style.css) بتصميم كلاسيكي نظيف ومتجاوب مع متغيرات ألوان وخطوط مخصصة ودعم كامل لـ RTL/LTR.

- [x] **الثيم الثاني - Minimal & Clean / Modern (Phase 68)**:
  - بناء وتصميم الثيم العصري الفاخر (Modern Theme) بمساحات بيضاء واسعة وتايبوغرافي قوي وحركات تفاعلية سلسة تناسب الماركات الفاخرة.

- [x] **الثيم الثالث - Vibrant & Dynamic / Bold (Phase 69)**:
  - بناء وتصميم الثيم الجريء والنابض بالحياة للموضة والملابس، مع صور هيرو ضخمة وشبكات عرض ديناميكية وحركات تفاعلية ودعم كامل لـ RTL/LTR.

- [x] **الثيم الرابع - Simple & Fast / Starter (Phase 70)**:
  - بناء وتصميم الثيم المبدئي السريع جداً للمبتدئين وبأقل عناصر ممكنة مع التركيز التام على السرعة القصوى ليكون مثالي للأجهزة الضعيفة.

- [x] **مكتبة مكونات الثيمات - Theme Components Library (Phase 71)**:
  - إنشاء 10 مكونات Blade قابلة لإعادة الاستخدام في جميع الثيمات (product-card, category-card, cart-drawer, search-bar, navbar, footer, breadcrumb, pagination, modal, toast) بدعم التخصيص عبر CSS Variables.

- [x] **نظام معاينة الثيمات - Theme Preview System (Phase 72)**:
  - بناء متحكم ThemePreviewController وواجهات المعاينة (Blade & React/Inertia) بدعم المعاينة الحية مع بيانات المتجر الفعلية والتخصيصات غير المحفوظة ومحاكي الأجهزة (Mobile, Tablet, Desktop).

- [x] **بنية سوق الثيمات - Theme Marketplace Foundation (Phase 73)**:
  - إنشاء صفحة تفاصيل الثيم والتقييمات بالـ Inertia React (`MarketplaceShow.jsx`).
  - إنشاء صفحات الـ Blade المقابلة لـ Fallback للماركت بليس وتفاصيل الثيم (`marketplace.blade.php` و `marketplace_show.blade.php`).
  - ربط وتفعيل الكنترولر `ThemeMarketplaceController.php` بنجاح وتوفير المسارات (Routes) اللازمة.

- [x] **تصميم الصفحة الرئيسية للمنصة الأم - fastorder.test (Phase 74)**:
  - تطوير وتجميل تصميم الصفحة الرئيسية (`platform/index.blade.php`) لتصبح بريميوم وتفاعلية.
  - دعم التبديل الفوري والديناميكي بين اللغتين (عربي/إنجليزي) والـ Dark/Light Mode.
  - تحديث الكنترولر `PlatformController.php` لتمرير البيانات المترجمة بديناميكية كاملة.

- [x] **صفحة الباقات والأسعار للمنصة الأم (Phase 76)**:
  - مراجعة وتطوير صفحة الباقات والأسعار في نسختي Inertia React (`Pricing.jsx`) والـ Blade (`pricing.blade.php`).
  - دمج وعرض جدول مقارنة المميزات الشامل ومفتاح تحويل الدفع الشهري والسنوي التفاعلي.

- [x] **تصميم الصفحات القانونية والثابتة للمنصة (Phase 77)**:
  - إنشاء وتصميم الـ Layout المشترك للمنصة الأم (`platform.blade.php`).
  - تصميم صفحات: من نحن (`about.blade.php`)، اتصل بنا مع نموذج تواصل تفاعلي (`contact.blade.php`)، سياسة الخصوصية (`privacy.blade.php`)، شروط الاستخدام (`terms.blade.php`)، اتفاقية مستوى الخدمة (`sla.blade.php`)، ومركز المساعدة (`help.blade.php`).

- [x] **نظام الإرجاع والاستبدال - Returns & Refunds (Phase 82)**:
  - إنشاء جدول مرتجعات الطلبات (`order_returns`) والموديل `OrderReturn.php` مع التوافق مع الـ Tenant scoping.
  - إعداد واجهة للعميل في حسابه بالمتجر لطلب الإرجاع والاستبدال.
  - إعداد واجهة للتاجر في لوحة التحكم لإدارة المرتجعات وإعادة المنتجات للمخزون تلقائياً.

- [x] **نظام منع الطلبات الوهمية - Ghost Order Blocker (Phase 80)**:
  - إنشاء جدول القائمة السوداء (`blacklist_records`) والموديل `BlacklistRecord.php` مع التوافق مع الـ Tenant scoping.
  - إعداد واجهة للتاجر لإدارة القائمة السوداء وإعدادات التحقق.
  - عمل Middleware للتحقق الذكي وحظر الطلبات الوهمية وتطبيقه على مسار الدفع.

- [x] **نظام إدارة المخزون المتقدم (Phase 81)**:
  - إنشاء جدول حركات المخزون (`stock_movements`) والموديل `StockMovement.php` مع التوافق مع الـ Tenant scoping.
  - إضافة حقل "حد التنبيه بانخفاض المخزون" في لوحة تحكم المنتجات للتاجر.
  - إرسال إشعار فوري للتاجر عند وصول المخزون للحد الحرج وتوفير صفحة تقارير المخزون وقيمتها.

- [x] **نظام التقييمات والتصنيفات للمتجر - Store Rating (Phase 86)**:
  - إنشاء جدول تقييمات المتجر (`store_ratings`) والموديل `StoreRating.php` مع التوافق مع الـ Tenant scoping.
  - إعداد واجهة للعميل لتقييم المتجر بعد إتمام الطلب بالنجوم والتعليق.
  - إعداد واجهة للتاجر لعرض وحساب المتوسطات وإدارة ظهور التقييمات.

- [x] **نظام استرداد السلات المتروكة - Abandoned Cart Recovery (Phase 85)**:
  - إنشاء جدول السلات المتروكة (`abandoned_carts`) والموديل `AbandonedCart.php` مع التوافق مع الـ Tenant scoping.
  - إعداد آلية تتبع وحفظ السلات المتروكة والأمر المجدول `TrackAbandonedCarts.php`.
  - إعداد قوالب البريد الإلكتروني وواجهة التاجر لإرسال تنبيهات التذكير بالاستعادة.

- [x] **نظام الـ Upsell والـ Cross-sell (Phase 79)**:
  - إنشاء جدول روابط المنتجات الموصى بها والموديل `ProductRecommendation.php` مع التوافق مع الـ Tenant scoping.
  - إعداد واجهة للتاجر لربط المنتجات كترقية (Upsell) أو مكملة (Cross-sell).
  - تطوير الـ Storefront لعرض منتجات الترقية عند الإضافة للسلة والمنتجات المكملة في السلة وبوابة الدفع.

- [x] **نظام الدردشة المباشرة للمتجر - Live Chat widget (Phase 87)**:
  - إضافة حقول الربط وإعدادات WhatsApp و Tawk.to في إعدادات لوحة التاجر.
  - تعديل الـ Storefront لحقن كود ويدجت الدردشة ديناميكياً بناءً على تفعيل التاجر.

- [x] **نظام القوائم المخصصة - Custom Menus (Phase 88)**:
  - إنشاء جدول القوائم (`menus`) والموديل `Menu.php` مع التوافق مع الـ Tenant scoping.
  - إعداد واجهة للتاجر لبناء وإدارة القوائم وسحب وإفلات العناصر والروابط.
  - تعديل الـ Storefront لجلب القوائم ورندرتها ديناميكياً في الهيدر والفوتر والـ sidebar.

- [x] **نظام التصدير والاستيراد الشامل (Phase 90)**:
  - إنشاء الخدمة `ImportExportService` لتوليد واستيراد البيانات وتكاملها مع WooCommerce و Shopify.
  - إنشاء واجهة للتاجر لاستيراد المنتجات والعملاء وتصديرها بصيغة CSV / Excel ومتابعة حالة التقدم.

- [x] **تحسين الأداء وقاعدة البيانات والذاكرة المؤقتة (Phase 92)**:
  - إضافة فهارس قاعدة البيانات المركبة لتسريع البحث والفلترة.
  - تطبيق طبقة كاش متكاملة `SettingService` وتخزين الإحصائيات مؤقتاً ومسح الكاش تلقائياً عند التحديث.
  - تجنب مشكلة الـ N+1 queries وتحسين أداء الرسم البياني والـ queries.

- [x] **إعداد بيئة الإنتاج وسيرفر الـ Production (Phase 95)**:
  - إعداد وتحسين ملف التكوين `.env.example` لبيئة الإنتاج ودعم الـ Wildcard subdomains و SSL.
  - إنشاء ملفات تكوين Nginx و Supervisor لإدارة الـ Web server والـ Queue workers باستمرار.
  - إنشاء سكربت النشر التلقائي المكتمل `deploy.sh` لتحديث قاعدة البيانات وتطهير الكاش وبناء الأصول.

- [x] **إعداد الـ Pipeline للنشر التلقائي - CI/CD Pipeline (Phase 96)**:
  - إنشاء ملف تكوين GitHub Actions في `.github/workflows/deploy.yml` لأتمتة الاختبارات والـ build.
  - تكامل النشر التلقائي الآمن عبر بروتوكول SSH لفرع الإنتاج باستخدام سكربت النشر.

- [x] **توافق سهولة الاستخدام - Accessibility WCAG 2.1 AA (Phase 94)**:
  - إضافة سمات الـ ARIA المناسبة في قوالب المتجر والمنصة وتحديد روابط التخطي السريع للمحتوى.
  - تفعيل مؤشر التركيز المخصص للتصفح باللوحة المفاتيح والتحكم بالألوان والتباين والنصوص البديلة للصور.

- [x] **كتابة توثيق المطورين ودليل استخدام التجار (Phase 97)**:
  - إنشاء دليل المطورين الشامل `docs/developer_guide.md` لمعمارية النظام وخطوات التشغيل محلياً وشرح الميزات.
  - إنشاء دليل التجار الشامل `docs/merchant_guide.md` لشرح ضبط المتجر، واستخدام الميزات المتقدمة كالمرتجعات والمخزون.

- [x] **نظام المراقبة والصيانة والدعم - Monitoring & Maintenance (Phase 99)**:
  - إعداد قناة تسجيل مخصصة `fastorder-errors.log` ومفسر الاستثناءات وجدولة الفحص الصحي.
  - إنشاء Artisan Command للمراقبة الصحية ورصد الاستعلامات البطيئة ومساحة القرص والـ Queue workers.
  - إرسال تنبيهات بريد إداري فوري لمدير النظام بقالب منسق وجذاب في حالة الفشل.

- [x] **بيانات تجريبية شاملة - Demo Data Seeding (Phase 98)**:
  - إنشاء Artisan Command المخصص `fastorder:seed-demo` لتوليد تينانت تجريبي بكل إعداداته.
  - توليد منتجات وأقسام وعروض ترقية وطلبيات موزعة وسجلات مخزن وسلات متروكة وقائمة سوداء لتمثيل نظام واقعي متكامل.

- [x] **المراجعة النهائية وقائمة الإطلاق - Launch Checklist (Phase 100)**:
  - إنشاء ملف قائمة فحص الإطلاق النهائي الشامل في `docs/launch_checklist.md`.
  - تأمين وتشفير الـ Cookies والـ Sessions لبيئة الإنتاج والتحقق من التكوين النهائي وقبول بوابات الدفع.

- [x] **الاختبار وضمان الجودة الجزء الثاني - QA & Testing (Phase 91)**:
  - إنشاء اختبارات آلية شاملة (Feature Tests) لتغطية الميزات الجديدة (السلات المتروكة، حظر الطلبات الوهمية، المرتجعات، وتوصيات المنتجات).
  - التأكد من نجاح تشغيل كافة الاختبارات وتمريرها بنسبة 100% (57 اختباراً).

- [x] **المراجعة الأمنية الشاملة - Security Audit (Phase 93)**:
  - تفعيل هيدرات الأمان بالكامل وعزل بيانات التينانت وعلاج ثغرات IDOR بالتحقق الإلزامي من التينانت عبر Scopes.
  - تأمين المدخلات باستخدام Form Requests، وتشفير الجلسات وحماية الـ Cookies بالإنتاج.

- [x] **تكامل وتفعيل شركات الشحن (Phase 101 - Shipping Gateways)**:
  - إضافة جدول `shipping_gateways` وجدول `shipments` وحفظ مفاتيح وحالات شركات الشحن.
  - بناء محرك درايفرات الشحن (`ShippingManager`) ودعم (بوسطة Bosta، J&T Express، والبريد المصري Egypt Post).
  - تطوير واجهة إدارة مفاتيح وربط الشحن بالـ React/Inertia وتوفير تدفق الربط التلقائي بضغطة زر (OAuth2) لـ J&T Express.
  - دمج خيارات إنشاء الشحنة وتوليد البوليسة (AWB) والتتبع اللحظي داخل تفاصيل الطلب بنقرة واحدة.
  - إضافة اختبارات آلية `ShippingGatewaysTest.php` واجتياز 100% من كافة اختبارات النظام (61 اختباراً ناجحاً).

